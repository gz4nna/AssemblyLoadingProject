# 将"控制台数据传输项目"改造为"可热插拔插件"的统一参考

本文件用于指导把现有的**独立控制台数据库同步程序**（如 `testprogram`）改造成可被
`AssemblyLoadingProject` 宿主热插拔调度的插件，从而无需单独部署/常驻即可定时执行。

## 0. 改造目标

| 改造前（控制台） | 改造后（插件） |
|---|---|
| 自带 `Main` 死循环 + 定时器 | 实现 `IDataTransferService`，单次执行逻辑 |
| 连接串硬编码在代码里 | 连接串/参数由宿主前端配置传入 |
| 需要单独部署进程常驻 | 放入 `Plugins` 目录，宿主统一调度 |
| `Console.WriteLine` 打印 | 使用 `PluginContext.Logger` 写入宿主日志 |

## 1. 需要改动的两处

### 1.1 csproj（关键：让插件复用宿主共享的 TransDataHelper）

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- 不需要 OutputType=Exe，Library 即可（可保留 Exe，不影响） -->
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- 关键：启用动态加载 -->
    <EnableDynamicLoading>true</EnableDynamicLoading>
  </PropertyGroup>

  <ItemGroup>
    <!-- 引用宿主以获取 IDataTransferService 契约（不复制，运行期由宿主提供） -->
    <ProjectReference Include="..\..\AssemblyLoadingProject\AssemblyLoadingProject\AssemblyLoadingProject.csproj">
      <Private>false</Private>
      <ExcludeAssets>runtime</ExcludeAssets>
    </ProjectReference>

    <!-- 引用 TransDataHelper（共享依赖，不复制，宿主统一提供驱动） -->
    <ProjectReference Include="..\..\DataTableCopyScripts\TransDataHelper\TransDataHelper\TransDataHelper.csproj">
      <Private>false</Private>
      <ExcludeAssets>runtime</ExcludeAssets>
    </ProjectReference>

    <!-- 项目原有的其它引用（如腾讯SDK）可保留；宿主没有的依赖需随插件部署 -->
  </ItemGroup>
</Project>
```

> **重要**：`Private=false` + `ExcludeAssets=runtime` 保证 `TransDataHelper.dll` 及驱动
> 不被复制进插件目录，运行期经 `AssemblyLoadContext.Default` 回退到宿主共享副本，
> 避免重复携带。**插件目录里不需要放置 TransDataHelper.dll**。

### 1.2 代码：把"死循环 Main"改成"实现 IDataTransferService"

把原来的 `Main` 里的**单次执行逻辑**抽成一个公开方法，然后实现 `IDataTransferService`：

```csharp
using AssemblyLoadingProject.Plugins.Abstractions;
using TransDataHelper.Adapters;
using TransDataHelper.Config.Connection;

namespace myplugin;

/// <summary>把单次执行逻辑放进 ExecuteAsync，由宿主定时调度。</summary>
public sealed class SyncPlugin : IDataTransferService
{
    public string Id => "myplugin.sync";
    public string DisplayName => "我的同步";
    public string Version => "1.0.0";
    public string Description => "源库->目标库的数据同步";

    // 连接参数改为从前端 Parameters 读取（不再硬编码）
    private SybaseAdapter? _src;
    private MySqlAdapter? _dst;

    public void Initialize(PluginContext ctx)
    {
        // 依据 ctx.Parameters 里的连接串构建适配器
        _src = new SybaseAdapter(new SybaseConnectionConfig
        {
            DataSource = ctx.Parameters.GetValueOrDefault("SybaseHost", "200.100.100.5"),
            Database = ctx.Parameters.GetValueOrDefault("SybaseDb", "mis200new"),
            User = ctx.Parameters.GetValueOrDefault("SybaseUser", "sa"),
            Password = ctx.Parameters.GetValueOrDefault("SybasePwd", ""),
            Port = ctx.Parameters.GetValueOrDefault("SybasePort", "5000"),
        });
        _dst = new MySqlAdapter(new MySqlConnectionConfig
        {
            DataSource = ctx.Parameters.GetValueOrDefault("MySqlHost", "200.100.100.245"),
            Database = ctx.Parameters.GetValueOrDefault("MySqlDb", "wx_nb200"),
            User = ctx.Parameters.GetValueOrDefault("MySqlUser", "root"),
            Password = ctx.Parameters.GetValueOrDefault("MySqlPwd", ""),
            Port = ctx.Parameters.GetValueOrDefault("MySqlPort", "3306"),
        });
        ctx.Logger("适配器已初始化", LogLevel.Info);
    }

    public async Task<TransferResult> ExecuteAsync(PluginContext ctx, CancellationToken ct)
    {
        // 把原来 ExecuteSyncTaskAsync() 的整段逻辑搬进来
        // 用 ctx.Logger 替代 Console.WriteLine
        // 返回 TransferResult.Ok/Fail，携带成功/失败与行数
        ctx.Logger("开始同步", LogLevel.Info);
        // ... 你的增删改查逻辑 ...
        ctx.Logger("同步完成", LogLevel.Info);
        return TransferResult.Ok("同步完成", rowsAffected: 100);
    }

    public void Dispose()
    {
        _src?.Dispose();
        _dst?.Dispose();
    }
}
```

## 2. 参考：`DataTransferPluginBase`

宿主内置了 `DataTransferPluginBase : IDataTransferService`，封装了"读源 → 分批写目标"
的通用流程（基于 TransDataHelper）。若你的同步逻辑符合该模式（从一个库查，写入另一个库），
可继承它，只需实现少量抽象成员：

```csharp
public sealed class MySyncPlugin : DataTransferPluginBase
{
    public override string Id => "mysync";
    public override string DisplayName => "MySQL→Sybase 同步";
    public override string Version => "1.0.0";
    public override string Description => "...";

    protected override string SourceDbType => "MySql";    // 源库类型
    protected override string TargetDbType => "Sybase";   // 目标库类型
    protected override string BuildSourceSql() => "SELECT ... WHERE flag=0";
    protected override string GetTargetTable() => "erp_dst";
    protected override string BuildRowSql(DataRow row) => $"INSERT INTO ... VALUES (...);";
}
```

若你的同步不是"简单查表写表"（涉及删旧插新、多步），**直接实现 `IDataTransferService` 更灵活**。

## 3. 部署与使用

1. 编译（Debug 即可），把输出的插件 DLL 放到 `AssemblyLoadingProject/bin/Debug/net10.0/Plugins/`。
   - **不要覆盖宿主目录里的 TransDataHelper.dll**（宿主已自带）。
2. 打开宿主 `/plugins` 页 → 重新扫描 → 找到该插件 → 展开配置参数与间隔 → 保存并启用。
3. 之后宿主按间隔自动调度执行；也可点"立即执行"手动触发。
4. 更新 DLL 后重新扫描会自动热重载。

## 4. 常见注意点

- **不要把 `Main` 死循环带进 `ExecuteAsync`**：宿主已按间隔调度，若插件内部再写 `while(true)+Timer` 会阻塞调度线程。`ExecuteAsync` 应为"一次执行"。
- **连接串不要硬编码**：尽量从 `ctx.Parameters` 读取，便于前端切换目标库。
- **注意数据库限制**：`TransDataHelper` 的 `OracleAdapter` 禁止写入（主库保护）；Sybase 走纯文本拼接，注意转义。
- **自行引入的第三方依赖**：宿主没有的 DLL 需一并放进插件目录（`Resolving` 会先查插件目录，再回退宿主）。
- **异常处理**：见下方"§4.1 异常与错误处理"——插件内部原则上不处理，直接抛出，由宿主统一识别处理。

### 4.1 异常与错误处理（插件内部不做处理，统一抛出）

**设计约定**：插件 DLL 内部**原则上不捕获异常、不自己兜底**。遇到任何错误（数据库连接失败、
数据校验不通过、推送失败等）都应**直接抛出**，交给宿主统一识别与处理。

- **宿主侧统一处理**：`PluginHostService.ExecutePluginAsync` 是唯一捕获异常的地方。插件抛出的异常会被：
  - 标记插件状态为 `Faulted`（异常）并在状态页展示原因；
  - 记录一条失败"执行历史"（时间/耗时/错误消息）；
  - 写入该插件的日志文件（`Plugins/logs/<插件名>.log`）；
  - 依据调度配置继续安排下次执行（失败不中断调度）。
- **插件内如何写**：`ExecuteAsync` 里不要 `try/catch` 后吞掉错误，直接让它向外抛：

```csharp
public async Task<TransferResult> ExecuteAsync(PluginContext ctx, CancellationToken ct)
{
    ctx.Logger("开始同步", LogLevel.Info);
    var dt = await LoadSourceAsync(ct);      // 这里出错会直接抛出
    WriteToTarget(dt);                        // 这里出错也会直接抛出
    ctx.Logger("同步完成", LogLevel.Info);
    return TransferResult.Ok("同步完成", rowsAffected: dt.Rows.Count);
}
```

- **建议的粒度**：可在"单个业务步骤"内记录一条上下文日志（`ctx.Logger`），但**不吞异常**。
  这样宿主日志里既能看到进度，又能看到最终错误，定位问题最方便。
- **取消（CancellationToken）**：长任务应响应 `ct`（如 `ct.ThrowIfCancellationRequested()`），
  宿主超时或停止时会抛出 `OperationCanceledException`，同样由宿主统一处理为"被取消/超时"。

### 4.2 单一职责：一个 DLL 原则上只承担一项职责

**设计约定**：一个插件 DLL **原则上只负责一项明确的任务**（例如"某表从源库同步到目标库"、
"定时生成报表并推送"、"定时同步考勤并推送通知"），不要在一个 DLL 里堆叠多个互不相关的功能。

- **原因**：每个 DLL 在宿主中是独立调度单元，有自己的启用/间隔/参数/日志/失败状态。
  一个插件只做一件事，配置与排错都更清晰，也便于单独启用、停用或热替换。
- **拆分建议**：若某项目包含多类任务，拆分为多个插件 DLL，各自实现 `IDataTransferService`。
  需要串行依赖的任务，可用 `Schedule` 的时间模式分别安排，或通过参数控制是否执行。
- **示例**：`AttendInfoPush` 原项目同时做了"读考勤数据"与"推送企业微信"两件事。若改造为插件，
  建议要么保持为"一个插件内串行完成读+推"（作为一项职责：考勤同步与推送），
  要么拆成"数据同步插件"+"消息推送插件"两个独立 DLL，按需启用。

## 5. 失败告警：邮件 / 企业微信推送参考

宿主目前统一把失败写入状态与日志；若需"失败即通知"，可在插件内调用内网推送服务
（或由宿主扩展统一告警）。下面给出**通用的调用方式**（地址/邮箱请换成你自己的配置，
**不要提交真实内网地址、个人邮箱与账号密码到远端仓库**）：

### 5.1 邮件告警（参考 `email_service.py`）

内网邮件服务是一个 HTTP 接口，`POST` JSON 即可发信（示例字段）：`Destination`（收件人）、
`Subject`（主题）、`TemplateID`（模板号）、`TemplateData`（模板变量 JSON 字符串）。
发送前需要 `using System.Net.Http.Json;`。

```csharp
using System.Net.Http.Json; // 提供 PostAsJsonAsync

// 以宿主配置提供地址与收件人，避免硬编码
var emailApi = ctx.Parameters.GetValueOrDefault("EmailApi", "http://<邮件服务地址>:<端口>/send");
var to = ctx.Parameters.GetValueOrDefault("EmailTo", "<你的邮箱>");

using var http = new HttpClient();
var payload = new
{
    Destination = new[] { to },
    Subject = "数据传输异常告警",
    TemplateID = 181112,
    TemplateData = "{\"wrong_level\":\"高\",\"wrong_type\":\"执行异常\"," +
                   "\"wrong_description\":\"插件执行失败\"}"
};
var resp = await http.PostAsJsonAsync(emailApi, payload);
if (!resp.IsSuccessStatusCode) ctx.Logger($"邮件发送失败 HTTP {resp.StatusCode}", LogLevel.Warn);
```

### 5.2 企业微信消息推送（参考 `AttendInfoPush`）

企业微信推送同样走内网 HTTP 接口，`POST` 一个含 `touser`（OA 账号）、`msgtype`（如 `textcard`）、
`textcard`（标题/描述/跳转 URL）的对象：

```csharp
var pushUrl = ctx.Parameters.GetValueOrDefault("PushUrl", "http://<推送服务地址>:<端口>/api/messagepush");
var oaAccount = ctx.Parameters.GetValueOrDefault("OaAccount", "<OA账号>");

using var http = new HttpClient();
var body = new
{
    touser = oaAccount,
    msgtype = "textcard",
    textcard = new
    {
        title = "数据通知",
        description = $"有新的数据，发送时间：{DateTime.Now}",
        url = ctx.Parameters.GetValueOrDefault("TargetUrl", "http://<业务页面地址>")
    }
};
var resp = await http.PostAsJsonAsync(pushUrl, body);
if (!resp.IsSuccessStatusCode) throw new Exception("企业微信推送失败");
```

> **安全提醒**：以上地址、邮箱、账号均为占位符。真实使用时请把内网 IP、个人邮箱、数据库口令
> 放到宿主前端参数或环境变量/`appsettings.*` 中，**严禁硬编码进插件源码并提交到远端仓库**。
